services:
  - type: web
    name: dcbot
    runtime: python
    buildCommand: pip install -r requirements.txt aiohttp
    startCommand: python bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: PORT
        value: 8080
